# app/api/v1/email.py

"""
Email API endpoints - Retained as minimal stub for compatibility, all client routes removed
"""

from fastapi import APIRouter

router = APIRouter()

# All previous routes removed as per custom mail replacement with SnappyMail
# System emails (e.g., user creation) are handled by email_service.py