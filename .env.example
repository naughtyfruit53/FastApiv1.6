# FastAPI ERP Application Environment Configuration

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/fastapi_erp

# Supabase Configuration (Required for Auth Integration)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# Security
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-email-password
EMAILS_FROM_EMAIL=your-email@gmail.com
EMAILS_FROM_NAME=Your App Name

# OAuth2 Configuration (for Email Integration)
# Google OAuth2 (Gmail Integration)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Microsoft OAuth2 (Outlook/Office365 Integration)
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
MICROSOFT_TENANT_ID=common

# OAuth2 Redirect URI
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# Data Encryption Keys (Required for OAuth2 token storage)
# Generate using: python -c "from cryptography.fernet import Fernet; import base64; print(f'ENCRYPTION_KEY_PII={base64.b64encode(Fernet.generate_key()).decode()}')"
ENCRYPTION_KEY_DEFAULT=your-base64-encoded-encryption-key
ENCRYPTION_KEY_PII=your-base64-encoded-pii-encryption-key
ENCRYPTION_KEY_FINANCIAL=your-base64-encoded-financial-encryption-key
SMTP_PASSWORD=your-app-password
EMAILS_FROM_EMAIL=your-email@gmail.com
EMAILS_FROM_NAME=TRITIQ ERP

# Alternative Email Services (Optional)
SENDGRID_API_KEY=your-sendgrid-api-key
BREVO_API_KEY=your-brevo-api-key
BREVO_FROM_EMAIL=your-email@domain.com

# WhatsApp OTP Configuration (Optional)
WHATSAPP_PROVIDER=brevo
WHATSAPP_SENDER_NUMBER=+1234567890
WHATSAPP_OTP_TEMPLATE_ID=123

# Redis (Optional)
REDIS_URL=redis://localhost:6379

# File Storage
UPLOAD_FOLDER=uploads
MAX_FILE_SIZE=10485760

# CORS Configuration (comma-separated list of allowed origins)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,https://fast-apiv1-6.vercel.app

# Super Admin Emails
SUPER_ADMIN_EMAILS=admin@tritiq.com,superadmin@tritiq.com

# Environment
ENVIRONMENT=development
DEBUG=true