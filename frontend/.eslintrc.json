{
  "extends": [
    "next/core-web-vitals",
    "plugin:@typescript-eslint/recommended",
    "plugin:react-hooks/recommended",
    "plugin:jest/recommended"
  ],
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "env": {
    "browser": true,
    "node": true,
    "es2021": true,
    "jest": true
  },
  "rules": {
    "@typescript-eslint/no-explicit-any": "warn",  // Warn instead of error for 'any' (fix gradually)
    "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_", "varsIgnorePattern": "^_" }],  // Ignore vars/args starting with '_'
    "no-undef": "error",  // Keeps globals check but env above will resolve most
    "react-hooks/exhaustive-deps": "warn",  // Warn for missing deps (fix as needed)
    "no-useless-escape": "error",
    "no-console": "warn"  // Warn on console logs (remove for production)
  },
  "overrides": [
    {
      "files": ["**/__tests__/**/*.[jt]s?(x)", "**/?(*.)+(spec|test).[jt]s?(x)"],
      "extends": ["plugin:jest/recommended"],
      "rules": {
        "react/display-name": "off",
        "@typescript-eslint/no-require-imports": "off"
      }
    }
  ]
}