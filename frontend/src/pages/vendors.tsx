import React from 'react';
import { useRouter } from 'next/router';
import VendorManagement from './masters/vendors';

const VendorsPage: React.FC = () => {
  return <VendorManagement />;
};

export default VendorsPage;