import React from 'react';
import { useRouter } from 'next/router';
import BOMManagement from './masters/bom';

const BOMPage: React.FC = () => {
  return <BOMManagement />;
};

export default BOMPage;